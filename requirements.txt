# ── LangChain ecosystem — fully compatible 0.3.x stable set ──────────────────
langgraph==0.2.70
langchain==0.3.25
langchain-groq==0.3.8
langchain-community==0.3.20
langchain-core==0.3.86
langchain-text-splitters==0.3.8

# PDF Parsing
pypdf==5.1.0
pdfminer.six==20231228

# Vector Store — uses built-in ONNX embeddings (no torch needed, fits in 512MB)
chromadb==0.5.23

# Groq LLM (free tier — no credit card needed)
groq==0.30.0

# FastAPI backend
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Streamlit frontend
streamlit==1.41.1
requests==2.32.3

# Utilities
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
aiofiles==24.1.0
